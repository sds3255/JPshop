spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name : org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true
# 스프링부트에서는 위의 별도 설정이 없으면 memory DB로 돌려버린다.
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
#    trace문은 테스트 할 때 sql문에 ?파라미터값이 무엇인지 표시해줌